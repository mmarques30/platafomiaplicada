

# Dados do Contrato sempre visíveis

Alterar o estado inicial do `Collapsible` de `false` para `true` em `src/pages/MeuSistemaDocumentos.tsx`.

## Alteração

**`src/pages/MeuSistemaDocumentos.tsx`** — mudar o valor default de `contratoOpen`:
```tsx
const [contratoOpen, setContratoOpen] = useState(true);
```

1 linha alterada, 1 arquivo.

