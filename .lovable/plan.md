

# Ajustar design dos Overview Cards

## Alteracao

### `src/components/meu-sistema/ProjetoOverviewCards.tsx`
- Centralizar conteudo dos cards (`text-center`, `items-center`)
- Diminuir tamanho da fonte do valor: de `text-lg` para `text-base font-semibold`
- Label: manter `text-[10px]` uppercase, centralizado
- Valor da fase atual: limitar tamanho (`text-sm`) para nao estourar o card
- Adicionar `py-5 px-3` para padding mais equilibrado
- Progress bar centralizada com `mx-auto max-w-[80%]`
- Cards com `rounded-xl` e sombra sutil `shadow-sm`

1 arquivo editado.

